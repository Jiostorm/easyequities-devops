services:
  gitea:
    image: gitea/gitea:1.22.6
    container_name: gitea
    restart: always
    environment:
      USER_UID: 1000
      USER_GID: 1000
    ports:
      - 2222:22
      - 3000:3000
    volumes:
      - ./data:/data
